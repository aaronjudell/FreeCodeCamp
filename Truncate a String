function truncate(str, num) {
  var truncStr = "";
  if(str.length <= num) {
      return str;
  } else {
      truncStr = str.slice(0, num - 3);
      truncStr = truncStr.concat("...");
      return truncStr;
  }
}
