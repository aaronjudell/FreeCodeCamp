function end(str, target) {
  //create a substring that's the end of the string, but the same length as target
  endOfString = str.substr(str.length - target.length);
  //check if end of string equals target parameter, returns true if so, otherwise false
  if(endOfString === target) {
    return true;
  } else {
    return false;
  }
}
